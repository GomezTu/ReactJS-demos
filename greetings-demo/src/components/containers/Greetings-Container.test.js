import React from 'react';
import { shallow, mount } from 'enzyme';

import { GreetingsContainer } from './GreetingsContainer';

function setup(){
    return mount(<GreetingContainer />);
}
describe('GreetingContainer', () => {
    it('', () => {

    });

})