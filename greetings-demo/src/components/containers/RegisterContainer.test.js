import React from 'react';
import { shallow, mount } from 'enzyme';

import { RegisterContainer } from './RegisterContainer';

function setup(){
    return mount(<RegisterContainer />);
}
describe('RegisterContainer', () => {
    it('', () => {

    });

})